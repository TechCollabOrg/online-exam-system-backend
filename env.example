# =============================================================================
# 先读这一段（人看的说明，程序不会自动读这个文件）
# =============================================================================
#
# 这个文件是「说明书」：告诉你有哪些「环境变量」的名字可以用。
# 你平时在自己电脑跑项目，多数情况不用管它——直接启动后端即可，
# 因为 application-dev.yml 里已经写了本机常用的默认值（例如 MySQL 账号 root / 密码 root）。
#
# 什么时候才需要环境变量？
#   ① 你的数据库密码不是 root，又不想改 yml 文件里写死的字；
#   ② 要把代码上传到 GitHub，但不想把真密码、真 API 密钥写进仓库。
#
# 怎么用（选一种你能做到的就行）：
#
#   【办法 A 最简单】Windows 搜索「编辑系统环境变量」→「环境变量」→
#   上面「用户变量」点「新建」→ 变量名填下面那种大写英文（例如 MYSQL_PASSWORD）
#   → 变量值填你的密码 → 全部确定 → 关掉再重新打开终端 / IDEA，再启动后端。
#
#   【办法 B】复制 env.local.example 为 env.local，填写真实密码（env.local 已 gitignore，勿提交）：
#   - IntelliJ：运行配置「ExamApplication」从 env.local 加载（需安装 EnvFile 插件，或手动在 Environment 里加载该文件）
#   - Cursor：F5 选「Backend: ExamApplication (dev)」，自动读取 env.local
#   - 命令行：.\start-dev.ps1
#   切勿把密码写进 .idea/runConfigurations、workspace.xml 等会提交的文件。
#
#   【办法 C】如果你用 IntelliJ IDEA：运行配置里找到「Environment」或「环境」，
#   把下面某一行的「名字=值」填进去（一行一个）。
#
# 下面每一行只是示例格式，前面的 # 表示注释；真正要生效时，在系统里填「名字」和「值」，
# 不要指望「复制这个文件改名为 .env」Spring 就会自动读——当前项目没加自动读 .env 的插件。
# =============================================================================

# --- MySQL（只有你的库名、账号、密码和默认不一样时才用）---
# MYSQL_URL=jdbc:mysql://127.0.0.1:3306/db_exam?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true&useSSL=false
# MYSQL_USERNAME=root
# MYSQL_PASSWORD=root

# --- Redis ---
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DATABASE=0

# --- JWT（想换登录令牌密钥时）---
# JWT_SECRET=请换成一长串随机字母数字

# --- 登录时密码传输加密用，必须和前端 VUE_APP_CRYPTO_KEY / VUE_APP_CRYPTO_IV 一样长，各 16 个字符 ---
# EXAM_AES_KEY=changeme16byte!!
# EXAM_AES_IV=changeme16byte!!

# --- 文件存储：local=写本地磁盘（默认 dev）；minio / aliyun=对象存储 ---
# STORAGE_TYPE=local
# LOCAL_UPLOAD_DIR=uploads/exam-files
# 本地存储时接口返回相对路径 /api/upload/files/...，无需再配置 base URL

# --- MinIO（仅当 STORAGE_TYPE=minio）---
# MINIO_ENDPOINT=http://127.0.0.1:9000
# MINIO_ACCESS_KEY=admin
# MINIO_SECRET_KEY=你的minio密码
# MINIO_BUCKET=online-exam

# --- 阿里云 OSS（只有你把存储改成 aliyun 才要填）---
# OSS_ACCESS_KEY_ID=
# OSS_ACCESS_KEY_SECRET=

# --- AI（只有你用 Coze / Dify / 大模型评分才要填）---
# CHAT_PLATFORM_TYPE=coze
# COZE_API_TOKEN=
# COZE_BOT_ID=
# DIFY_API_KEY=
# LLM_API_KEY=

# --- Milvus（很少作业会用到，可忽略）---
# MILVUS_HOST=127.0.0.1
# MILVUS_PORT=19530
